services:
  nginx:
    image: nginx:latest
    container_name: production_nginx
    volumes:
      - /home/devops1/devel/gitrepos/IntegrationA/nginx/nginx.conf:/etc/nginx/nginx.conf
      - /home/devops1/devel/gitrepos/IntegrationA/nginx/:/etc/letsencrypt/
    ports:
      - 80:80
      - 443:443
  order:
    image: ${ORDER_IMAGE}
    expose:
      - "8080"
    volumes:
      - order-logs:/usr/local/tomcat/logs
      
volumes:
  order-logs: {}
